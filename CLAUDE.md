# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

기업상품(인터넷, 전용회선, IDC, TV 등) 소개 및 고객 유치용 홈페이지 **제작 중인 템플릿**.
상담/신청 유입을 유도하는 것이 목적.

**현재 특정 통신사와 제휴/계약된 상태가 아니다.** 페이지, 데이터, 문서 어디에도 특정 통신사명이나
"공식 대리점" 등 특정 사업자를 지칭·암시하는 표현을 넣지 않는다 (실제 사업자가 정해지기 전까지는
누구의 공식 채널도 아닌 것으로 명확히 보여야 오인 소지가 없다). 상품명은 실제 통신 시장에 존재하는
서비스 구조를 참고해 작성되었을 수 있으나, 이는 어디까지나 콘텐츠 초안이며 특정 사업자의 정식 취급
상품임을 뜻하지 않는다.

## 비즈니스 컨텍스트 (중요)

- **기업상품은 소비자(가정용) 상품과 달리 라이프사이클이 다르다.**
  - 신규 상품이 주기적으로 출시된다.
  - 기존 상품은 단종되지 않고 **장기간 병행 유지**되는 경우가 많다 (구상품 가입 고객도 계속 존재).
  - 따라서 "최신 상품 몇 개만 보여주고 나머지는 갈아엎는" 구조는 부적합하다.
- 이 특성 때문에 **상품 데이터는 페이지 마크업과 분리해서 관리**한다 (아래 데이터 구조 원칙 참고).
  신상품 추가 = 데이터 항목 추가, 구상품 유지 = 데이터 항목을 지우지 않고 상태만 관리.
- 특정 통신사 로고/브랜드 자산은 사용하지 않는다. 제휴가 확정되면 그때 다시 논의한다.

## 기술 스택

정적 웹페이지 (HTML / CSS / Vanilla JS)만으로 구성한다. 서버, 빌드 도구, DB, 프레임워크는 사용하지 않는다.
랜딩페이지 특성상 로딩 속도와 배포 단순성이 고객 유입(마케팅)에 직결되므로, 이 구조를 임의로 확장하지 않는다.

- 상품 데이터: `data/products.json` + 클라이언트 JS(`js/main.js`)가 fetch로 읽어 렌더링.
- 업종 데이터: `data/industries.json` + `js/main.js`가 fetch로 읽어 렌더링 (아래 아키텍처 참고).
- 상담 폼 전송 로직은 아직 미연결 상태 (`js/main.js`의 submit 핸들러 참고). 실제 전송 연동은 별도 논의 후 진행.

## 개발 명령어

빌드/린트/테스트 도구가 없는 순수 정적 사이트다. 로컬 확인은 정적 서버로만 가능하다
(`data/*.json`을 `fetch()`로 읽으므로 `file://`로 직접 열면 CORS 에러가 난다).

```bash
python -m http.server 8000   # 프로젝트 루트에서 실행 후 http://localhost:8000 접속
```

수정 후 검증 방법은 아래 "검증 프로세스" 섹션을 따른다.

## 아키텍처

`index.html` 하나로 동작하는 탭 기반 SPA다. 페이지 이동은 서버 라우팅이 아니라
`js/main.js`의 `showTab()` / `showDetail()`이 DOM의 `hidden` 속성을 토글하는 방식으로 처리한다.

- **탭 패널 3개** (`#industries`, `#products`, `#support`) + **상세 오버레이 1개** (`#detail`)가
  `<main class="tab-panels">` 안에 모두 존재하며, 활성 패널만 보이고 나머지는 `hidden`으로 숨겨진다.
- **해시 기반 라우팅**: `showTab()`이 `history.replaceState`로 `#industries` 등 해시를 갱신하고,
  최초 로드 시 `window.location.hash`를 읽어 시작 탭을 결정한다 (`js/main.js` 하단 `initialTab`).
  상세 화면 진입 시에는 `#detail-<returnTab>` 형태로 기록해 뒤로가기 시 돌아갈 탭을 함께 저장한다.
- **데이터 흐름**: `init()`이 `data/industries.json`과 `data/products.json`을 각각 fetch해서
  `renderIndustries()` / `renderProducts()`에 전달한다. 두 렌더러 모두 카드 클릭 시 `showDetail()`을
  호출해 같은 상세 오버레이 마크업을 재사용한다 (업종의 서비스 카드 상세, 상품 카드 상세 공통).
- **상품 목록 그룹핑**: `renderProducts()`는 활성 카테고리가 "전체"일 때는 `category`로,
  특정 카테고리를 선택했을 때는 `group`으로 하위 그룹을 묶어 렌더링한다 (`groupKeyOf()` 참고).
  카테고리 필터 버튼(`renderFilters()`)은 `products.json`의 `category` 값에서 자동 생성되므로
  새 카테고리를 추가해도 마크업을 건드릴 필요가 없다.
- **CSS**: 별도 프레임워크 없이 `css/style.css` 하나로 관리하며, 색상/반경 등은 `:root`의
  CSS 커스텀 프로퍼티(`--color-primary` 등)로 정의한다. 모바일 대응은 `max-width: 600px`
  미디어쿼리 한 곳에 모여 있다.

## 상품 데이터 구조 원칙

- 상품 정보는 `data/products.json`에 저장한다. HTML에 하드코딩하지 않는다.
- 각 상품 항목은 최소한 다음 필드를 가진다:
  - `id`, `name`, `category` (인터넷/전용회선/IDC/TV 등)
  - `status` (`active` | `legacy`) — 단종이 아니라 "신규 판매 중" vs "기존 유지"의 구분
  - `launchDate` — 출시(등록)일. 최신순 정렬에 사용
  - `description`, `price`, `spec` 등 소개용 필드
  - `group` (선택) — 소분류. 카테고리 선택 시 이 이름으로 소제목이 생김
- 상품을 교체할 때 기존 항목을 삭제하지 말고 `status`를 변경한다. 삭제는 실제로 안내를 완전히 중단할 때만.
- 목록 페이지는 이 데이터 파일을 읽어 렌더링하는 구조를 유지한다 (신상품 추가 시 마크업을 건드리지 않도록).
- 필드 상세 설명, 신규 상품 추가/삭제 절차, 요금 표기 규칙은 `data/README.md`에 정리되어 있다 —
  `products.json`을 수정하기 전에 먼저 확인한다.
- 업종 데이터(`data/industries.json`)는 `id`, `name`, `description`, `services`(문자열 배열) 구조를
  따르며, 업종별로 노출할 서비스 목록만 관리한다. 상품과 달리 `status`/`launchDate` 개념은 없다.

## 코딩 컨벤션

- 파일/폴더는 소문자-하이픈(kebab-case).
- 상품 데이터 필드명은 camelCase로 통일.
- 인라인 스타일 지양, CSS는 별도 파일로 분리.
- 주석은 "왜"가 비직관적일 때만 작성 (예: status 필드를 지우지 않는 이유 등).

## 검증 프로세스 (코드 수정 후 필수)

`data/products.json`, `data/industries.json`, `js/main.js`, `css/style.css`, `index.html` 중
하나라도 수정했다면, "완료"로 보고하기 전에 반드시 아래 절차로 실제 렌더링을 확인한다. 타입 체크나
문법 확인만으로는 기능이 맞다는 근거가 되지 않는다.

1. **정적 서버 실행** — `python -m http.server <port>` (또는 동급 정적 서버).
   상품 데이터를 `fetch()`로 읽는 구조이므로 `file://`로 직접 열면 CORS 에러가 나서 검증이 안 된다.
2. **Headless 브라우저로 접속** (Playwright 등) 후 다음을 확인:
   - **렌더링** — 헤더/히어로/업종그리드/상품목록/상담폼/푸터가 실제로 그려지는지, 상품 카드가
     `data/products.json` 항목 수와 맞게 나오는지.
   - **클릭 반응** — 카테고리 필터 버튼 클릭 시 목록이 실제로 갱신되는지, 앵커 링크(`#products`,
     `#contact`)로 이동하는지, 상담 폼 입력 필드에 값이 들어가는지.
   - **콘솔 에러 유무** — JS 예외, 실패한 fetch, 404 등 콘솔 에러가 하나도 없어야 한다. 눈에 안
     보이는 에러도 버그다.
   - **모바일 반응형** — viewport를 모바일(375px 너비)과 데스크톱(1280px 너비) 두 크기로 바꿔가며
     스크린샷을 찍어 레이아웃 깨짐이나 가로 스크롤이 없는지 확인한다.
3. **특정 통신사 표현 유입 검증** — 렌더링된 페이지, `data/*.json`, 커밋 내용에 특정 통신사명이나
   "공식 대리점" 등 특정 사업자를 암시하는 표현이 새로 들어가지 않았는지 확인한다. 제작 중 임시
   참고 자료를 그대로 복붙하다가 섞여 들어가기 쉬우므로 매번 확인 대상에 포함한다.
4. 스크린샷을 찍었다면 반드시 열어서 눈으로 확인한다 — 찍기만 하고 확인하지 않으면 검증한 것이
   아니다.

## 마케팅/컴플라이언스 규칙

- **모바일 반응형 필수.** 기업 고객 유입의 상당수가 모바일에서 발생하므로, 새 섹션/컴포넌트
  추가 시 375px 너비에서 깨지지 않는지 항상 확인한다 (위 검증 프로세스 참고).
- **특정 통신사 지칭 금지.** 제휴가 확정되기 전까지 페이지 텍스트, 상품 데이터, 링크 어디에도
  특정 통신사명이나 "공식 대리점" 등 특정 사업자를 지칭·암시하는 표현을 넣지 않는다. 대신 제작
  중임을 나타내는 문구(현재는 히어로 배지·푸터)를 유지한다.
- **가격/스펙은 데이터 파일만 수정.** 실제 최신 공지 기준으로 값이 바뀌어도 `data/products.json`만
  고치면 되도록 구조를 유지한다.
- **개인정보 수집 시 처리방침 필수.** 상담 신청 폼이 실제로 개인정보를 수집하게 되면(연락처 등),
  개인정보 처리방침 페이지/문구를 반드시 추가한다.
