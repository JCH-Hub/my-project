# 프로젝트 개요

SK브로드밴드 정식 대리점의 **기업상품 소개 및 고객 유치용 홈페이지**.
공식 계약을 맺은 대리점으로서 SK브로드밴드 기업상품(인터넷, 전용회선, IDC, TV 등)을 소개하고
상담/신청 유입을 유도하는 것이 목적.

# 비즈니스 컨텍스트 (중요)

- **기업상품은 소비자(가정용) 상품과 달리 라이프사이클이 다르다.**
  - 신규 상품이 주기적으로 출시된다.
  - 기존 상품은 단종되지 않고 **장기간 병행 유지**되는 경우가 많다 (구상품 가입 고객도 계속 존재).
  - 따라서 "최신 상품 몇 개만 보여주고 나머지는 갈아엎는" 구조는 부적합하다.
- 이 특성 때문에 **상품 데이터는 페이지 마크업과 분리해서 관리**한다 (아래 데이터 구조 원칙 참고).
  신상품 추가 = 데이터 항목 추가, 구상품 유지 = 데이터 항목을 지우지 않고 상태만 관리.
- 대리점이므로 공식 로고/브랜드 자산을 사용할 수 있으나, **"정식 판매점/대리점"임을 명확히 표기**한다
  (SK브로드밴드 본사 공식 홈페이지로 오인되지 않도록 - 통신 대리점 업계의 일반적 표기 관행).

# 상품 데이터 구조 원칙

- 상품 정보는 `data/products.json` (또는 유사 데이터 파일)에 별도 저장한다. HTML에 하드코딩하지 않는다.
- 각 상품 항목은 최소한 다음 필드를 가진다:
  - `id`, `name`, `category` (인터넷/전용회선/IDC/TV 등)
  - `status` (`active` | `legacy`) — 단종이 아니라 "신규 판매 중" vs "기존 유지"의 구분
  - `launchDate` — 출시(등록)일. 최신순 정렬에 사용
  - `description`, `price`, `spec` 등 소개용 필드
- 상품을 교체할 때 기존 항목을 삭제하지 말고 `status`를 변경한다. 삭제는 실제로 안내를 완전히 중단할 때만.
- 목록 페이지는 이 데이터 파일을 읽어 렌더링하는 구조로 만든다 (신상품 추가 시 마크업을 건드리지 않도록).

# 기술 스택

- 정적 사이트 (HTML/CSS/Vanilla JS) 기본. 별도 서버/빌드 도구가 필요 없는 랜딩페이지 특성상 가장 적합.
  - 로딩 속도, 배포 단순성이 고객 유입(마케팅) 목적에 유리.
- 상품 데이터는 JSON 파일 + 클라이언트 JS로 렌더링 (또는 빌드 타임 정적 생성 스크립트).
- 상담/신청 폼이 필요해지면 폼 전송은 별도 백엔드 없이 처리 가능한 방식(예: 이메일 서비스, 폼 수집 서비스)을 우선 검토하고,
  자체 서버가 필요한 시점에 재논의한다.

# 코딩 컨벤션

- 파일/폴더는 소문자-하이픈(kebab-case).
- 상품 데이터 필드명은 camelCase로 통일.
- 인라인 스타일 지양, CSS는 별도 파일로 분리.
- 주석은 "왜"가 비직관적일 때만 작성 (예: status 필드를 지우지 않는 이유 등).

# 브랜드/컴플라이언스 체크리스트

- 페이지 어딘가(푸터 등)에 "본 사이트는 SK브로드밴드 공식 판매 대리점입니다" 등 대리점 표기 문구 명시.
- 상품 가격/스펙은 실제 최신 공지 기준으로 업데이트 — 변경 시 데이터 파일만 수정하면 되도록 구조 유지.
- 개인정보 수집(상담 신청 폼)이 들어가면 개인정보 처리방침 페이지/문구 필수 (통신 대리점은 개인정보 수집이 흔함).
